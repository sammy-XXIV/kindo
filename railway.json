{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd mini-app && npm ci && npm run build && cd ../server && npm ci"
  },
  "deploy": {
    "startCommand": "node server/src/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
